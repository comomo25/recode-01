# 街歩き記録アプリ 機能仕様書

**最終更新日: 2025-10-27**

---

## 1. アプリ概要

### コンセプト
街歩き中に、その場で写真・テキスト・音声を記録し、地図上のピンとして保存。すべてのユーザーの記録が1つの地図上に表示され、他の人の街歩き体験も共有できるソーシャル記録アプリ。

### ターゲットデバイス
- スマートフォン（メイン）
- レスポンシブ対応でデスクトップでも閲覧可能

### 対象地域
- 日本全国

---

## 2. 主要機能

### 2.1 初回登録
- ニックネームの登録（必須）
- ブラウザに秘密のユーザーID（UUID）を自動生成・保存
- ユーザー認証は不要
- ニックネームは後から変更可能（自分の投稿との紐付けは維持）

### 2.2 地図表示
- 日本地図を表示（起動時は現在地中心）
- 全ユーザーの投稿がピンで表示される
- ピンの種類：
  - 写真のみ
  - テキストのみ
  - 音声のみ
  - 複数メディア（組み合わせ）
- ピンをタップすると投稿内容を表示

### 2.3 記録の投稿
**投稿できる情報：**
- 位置情報（GPS自動取得）
- 写真（1枚まで、推奨サイズ: 最大5MB）
- テキストメモ（文字数制限: 500文字）
- 音声録音（最大5分）
- 投稿日時（自動記録）

**投稿方法：**
- 地図画面に「＋」ボタン（固定配置）
- タップすると投稿モーダル表示
- 写真・テキスト・音声のいずれか1つ以上必須
- 現在地を自動取得（位置情報許可必須）
- 「投稿」ボタンで確定

### 2.4 記録の閲覧
- ピンをタップすると詳細モーダル表示
- 表示内容：
  - ニックネーム
  - 投稿日時
  - 写真（あれば表示）
  - テキスト（あれば表示）
  - 音声再生プレーヤー（あれば表示）
  - 編集・削除ボタン（本人の投稿のみ）

### 2.5 記録の編集・削除
**編集：**
- 本人の投稿のみ編集可能（ブラウザの秘密IDで照合）
- テキストのみ編集可能
- 写真・音声は再アップロードで差し替え
- 位置情報は変更不可

**削除：**
- 本人の投稿のみ削除可能
- 確認ダイアログ表示後に削除

---

## 3. ユーザーフロー

### 初回訪問時
1. アプリ起動
2. ニックネーム登録モーダル表示
3. ニックネーム入力
4. 秘密IDを自動生成してブラウザに保存
5. 位置情報許可をリクエスト
6. モーダルを閉じて地図画面へ

### 2回目以降
1. アプリ起動
2. 保存済みの秘密IDを確認
3. 地図画面を直接表示

### 投稿フロー
1. 地図画面で「＋」ボタンをタップ
2. 投稿モーダル表示
3. 現在地を自動取得
4. 写真撮影/アップロード（任意）
5. テキスト入力（任意）
6. 音声録音（任意）
7. 「投稿」ボタンをタップ
8. モーダルを閉じて地図上にピンが追加される

### 閲覧フロー
1. 地図上のピンをタップ
2. 投稿詳細モーダル表示
3. 写真閲覧/音声再生/テキスト読む
4. モーダルを閉じる

### 編集フロー
1. 自分の投稿のピンをタップ
2. 投稿詳細モーダル表示
3. 「編集」ボタンをタップ
4. 編集モーダル表示（既存データ入り）
5. 内容を修正
6. 「更新」ボタンをタップ
7. モーダルを閉じる

---

## 4. データ構造

### ユーザー情報
```
{
  userId: UUID (ブラウザに保存),
  nickname: String
}
```

### 投稿情報
```
{
  postId: UUID,
  userId: UUID,
  nickname: String (投稿時点のニックネーム),
  latitude: Number,
  longitude: Number,
  photoUrl: String (optional),
  textMemo: String (optional, max 500文字),
  audioUrl: String (optional),
  audioDuration: Number (optional, 秒),
  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

---

## 5. 画面構成（モーダル中心設計）

### メイン画面：地図画面（唯一の画面）
**常時表示要素：**
- 地図表示エリア（全画面）
- 全投稿のピン
- 「＋」ボタン（固定配置、右下推奨）
- 「現在地」ボタン（地図を現在地に移動）
- メニューボタン（右上または左上）

**モーダル/ドロワー：**
すべての機能はモーダルで表示

---

### モーダル1: 初回登録モーダル
**表示タイミング：** 初回訪問時のみ

**内容：**
- アプリ説明（簡潔に）
- ニックネーム入力フィールド
- 「はじめる」ボタン

---

### モーダル2: 投稿作成モーダル
**表示タイミング：** 「＋」ボタンタップ時

**内容：**
- モーダルヘッダー（「新しい記録」）
- 位置情報表示（住所または座標）
- 写真アップロード/カメラ起動ボタン（プレビュー表示）
- テキスト入力エリア（500文字まで）
- 音声録音ボタン + 録音中インジケーター + 再生プレーヤー
- 「投稿」ボタン（下部固定）
- 「×」閉じるボタン（ヘッダー右上）

**動作：**
- 「投稿」タップ → モーダルを閉じて地図にピン追加
- 「×」タップ → 確認ダイアログ後にモーダルを閉じる

---

### モーダル3: 投稿詳細モーダル
**表示タイミング：** ピンタップ時

**内容：**
- モーダルヘッダー（ニックネーム + 投稿日時）
- 写真（あれば表示、タップで拡大）
- テキスト（あれば表示）
- 音声プレーヤー（あれば表示、再生/停止ボタン + シークバー）
- 「編集」「削除」ボタン（本人の投稿のみ表示）
- 「×」閉じるボタン（ヘッダー右上）

**動作：**
- 「編集」タップ → 編集モーダル表示
- 「削除」タップ → 確認ダイアログ → 削除 → モーダルを閉じる
- 「×」タップ → モーダルを閉じる

---

### モーダル4: 編集モーダル
**表示タイミング：** 投稿詳細モーダルで「編集」タップ時

**内容：**
- 投稿作成モーダルと同じUI
- モーダルヘッダーは「記録を編集」
- 既存データが初期表示される
- 「更新」ボタン（下部固定）
- 「×」閉じるボタン

**動作：**
- 「更新」タップ → モーダルを閉じて地図に反映
- 「×」タップ → 確認ダイアログ後にモーダルを閉じる

---

### モーダル5: 設定ドロワー/モーダル
**表示タイミング：** メニューボタンタップ時

**内容：**
- 現在のニックネーム表示
- ニックネーム変更入力フィールド + 「変更」ボタン
- アプリ情報
- 利用規約/プライバシーポリシーへのリンク
- 「×」閉じるボタン

---

## 6. 技術的制約・要件

### データ制限
- 音声: 最大5分
- 写真: 最大5MB、1枚まで
- テキスト: 最大500文字

### 必要な権限
- 位置情報（GPS）
- カメラ（写真撮影時）
- マイク（音声録音時）

### オフライン対応
- 今回の実装では対応しない（将来的な拡張性として検討）

### バックエンド候補
- Firebase または Supabase
  - Firebase: Firestore + Storage + Hosting
  - Supabase: PostgreSQL + Storage + Edge Functions

---

## 7. 非機能要件

### セキュリティ
- 秘密IDはローカルストレージに保存（localStorage）
- 不適切な投稿への対応方法（将来的に通報機能など検討）

### パフォーマンス
- 地図上のピン数が多い場合はクラスタリング表示
- 画像は圧縮してアップロード

### ユーザビリティ
- スマートフォンでのタップ操作に最適化
- ローディング表示
- エラーハンドリング（位置情報取得失敗、アップロード失敗など）
- モーダルは下からスライドインのアニメーション推奨

---

## 8. 今後の拡張性（スコープ外）

- オフライン対応（PWA化）
- フィルター機能（期間、ユーザー、メディアタイプ）
- いいね・コメント機能
- 通報・ブロック機能
- ルート記録（歩いた経路を線で記録）
- 投稿への複数写真対応
- ハッシュタグ機能

---

## 変更履歴

### 2025-10-27
- 初版作成
- 画面構成をモーダル中心設計に変更
